<Window x:Class="WpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <!--TextBlock x:Name="textBlock" Grid.Column="0" Grid.ColumnSpan="4" /-->
        <Button x:Name="btnGet" Content="Get" Click="Button_Click" Grid.Column="0" Grid.Row="0" />
        <Button x:Name="btnPost" Content="Post" Click="BtnPost_Click" Grid.Column="1" Grid.Row="1" />
    </Grid>
</Window>
